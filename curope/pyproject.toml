[build-system]
requires = [
  "setuptools",
  "wheel",
  "torch",
  "ninja"
]
build-backend = "setuptools.build_meta"

[project]
name = "curope"
version = "1.0.0"
requires-python = ">=3.7"
dependencies = [
  "torch"
]